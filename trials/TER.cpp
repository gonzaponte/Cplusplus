#include "nr3.h"
#include "ran.h"
#include <iostream>
#include <fstream>
#include <conio.h>

using namespace std;
int main(void)
{
    class txt
    {
    public:
           vector<int> pos(3);
           int fichas;
           vector<vector<int>> p(5);
           int txt.fichas();
           vector<vector<int>> posicions();
    }
    class tab
    {
          vector < int, int, char > casilla;
    public:
           void modpos(vector < int, int, char > xog);
    }
    void modpos(<vector <int,int,char> xog)
    {
         tab.casilla = xog;
    }
    txt A,B;
    A.fichas=5;B.fichas=5;
    for (int i=0; i<5 ; i++)
    {
        for (int j=0; j<3 ; j++) A.pos[j]=0;
        A.posicions[i] = A.pos;
    }
    for (int i=0; i<5 ; i++)
    {
        for (int j=0; j<3 ; j++) B.pos[j]=0;
        B.posicions[i] = A.pos;
    }
}
