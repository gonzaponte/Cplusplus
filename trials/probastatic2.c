#include <stdio.h>
#include "probastatic.h"
int main(void)
{
     int contador=0;
     while (contador<100)
     {
           contar();
           contador++;
     }
     system("pause");
     return(0);
}
